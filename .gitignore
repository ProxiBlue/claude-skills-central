*.bak*
*~
__pycache__/
*.pyc
scripts/bin/
.mcp.json.bak*
# (removed dead dir entry)
infra/do-bugsink/.bugsink-secret
infra/do-bugsink/.admin-password

# Vendored MCP servers cloned+built locally (own .git, node_modules) — not
# fleet-repo content, regenerate per the header comment in the launcher
# script that references each (e.g. scripts/magento-standards-mcp.sh).
scripts/vendor/
