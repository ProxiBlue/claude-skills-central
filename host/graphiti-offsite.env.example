# Off-site backup target — copy to ~/.config/graphiti-offsite.env once the
# rclone 'b2' remote is configured (see RECOVERY.md). Backblaze B2 chosen
# 2026-08-02 after Icedrive DISCONTINUED WebDAV (no headless path left).
RCLONE_REMOTE="b2:proxiblue-backups/graphiti"
OFFSITE_RETENTION=14
